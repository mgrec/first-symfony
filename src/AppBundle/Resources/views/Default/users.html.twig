<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Document</title>
</head>
<body>

<ul>
    {% for user in users %}
    <li>
        {{ user.name }} - {{ user.lastname }} - {{ user.email }} -  <a href="{{ path('app_userSingle', { 'id': user.id }) }}">view</a>
    </li>
    {% endfor %}
</ul>

</body>
</html>